import { PlaceholderPage } from "../../components/layout/PlaceholderPage";

// 枠だけ（scoring.config の表示は Slice 2+）。
export default function SettingsPage() {
  return <PlaceholderPage title="Settings" description="設定（中身は後続スライスで実装）。" />;
}
