import { PlaceholderPage } from "../../components/layout/PlaceholderPage";

// 既定ランディング。本実装は task-18 が置き換える。
export default function InboxPage() {
  return (
    <PlaceholderPage
      title="Inbox Triage"
      description="未トリアージのシグナルをさばく既定ランディング。"
    />
  );
}
